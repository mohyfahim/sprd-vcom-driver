PACKAGE_NAME="sprd-vcom"
PACKAGE_VERSION="1.0.0"

BUILT_MODULE_NAME[0]="sprd_vcom"
DEST_MODULE_LOCATION[0]="/kernel/drivers/usb/serial"
AUTOINSTALL="yes"

MAKE[0]="make KDIR=/lib/modules/${kernelver}/build module"
CLEAN="make KDIR=/lib/modules/${kernelver}/build clean"
